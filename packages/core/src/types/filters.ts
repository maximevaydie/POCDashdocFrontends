export type RequestFilters = {
    site: number;
    start: string;
    end: string;
};

export type AvailabilityFilters = {
    zone: number;
    start: string;
    end: string;
};
export type ReportingFilters = {
    site: number;
};
